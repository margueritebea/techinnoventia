DEBUG=True
SECRET_KEY=changeme!
DATABASE_URL=postgres://postgres:password@localhost:5432/tech_innoventia_db
# Fichier .env.example
# Copier ce fichier en .env et remplir avec vos vraies valeurs
# IMPORTANT : Ne JAMAIS commit le fichier .env dans Git !

# Environnement (dev ou production)
DJANGO_ENV=production

# Clé secrète Django (générer avec: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())')
DJANGO_SECRET_KEY=votre-cle-secrete-super-longue-et-aleatoire

# Configuration de la base de données PostgreSQL
DB_NAME=nom_de_la_base
DB_USER=utilisateur_db
DB_PASSWORD=mot_de_passe_secure
DB_HOST=localhost
DB_PORT=5432

# Configuration email
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=votre-email@gmail.com
EMAIL_HOST_PASSWORD=votre-mot-de-passe-app
DEFAULT_FROM_EMAIL=noreply@votre-domaine.com

# Email administrateur
ADMIN_EMAIL=admin@votre-domaine.com

# Redis (optionnel, pour le cache)
# REDIS_URL=redis://127.0.0.1:6379/1